import React, { useState } from 'react';

export const MainMenu: React.FC = () => {
    const SubMenu = ['1111', '2222', '3333'];

    const [isMouseOnMenu, setIsMouseOnMenu] = useState(false);

    const menu: Array<JSX.Element> = [];

    return <></>;
};
